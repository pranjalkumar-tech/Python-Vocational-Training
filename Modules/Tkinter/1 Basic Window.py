# Tkinter Basic Window

import tkinter as tk  # Imports tkinter

window = tk.Tk()  # Creates the window
window.title("My First GUI")  # Sets the window title
window.geometry("400x300")  # Sets the window size
window.mainloop()  # Runs the window