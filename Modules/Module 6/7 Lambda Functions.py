# Lambda Functions
# A lambda function is a small anonymous function.
# It can have any number of arguments but only one expression.

square = lambda x: x * x  # Returns the square of a number
print(square(5))
add = lambda a, b: a + b  # Returns the sum of two numbers
print(add(10, 20))